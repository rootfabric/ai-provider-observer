# Project Control

Harness revision: **HYBRID-HARNESS-R12**

Template state: `HARNESS_READY`; active mission отсутствует.

Source of truth: Git + `config/control/` + durable evidence.

R12 preserves R8–R11 proof invariants and adds a token-bounded agent API: compact `brief`/`diagnose`, bounded default output, durable raw logs, compressed verifier transport, and atomic candidate/verifier/event transitions.
